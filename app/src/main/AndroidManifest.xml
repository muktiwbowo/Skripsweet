<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muktiwbowo.skripsweet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.WithoutActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".obat.ObatActivity"
            android:label="@string/obat_name"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".apoteker.AdminActivity"
            android:label="@string/title_activity_admin"
            android:theme="@style/AppTheme.WithoutActionBar" />
        <activity
            android:name=".kasus.KasusActivity"
            android:label="Kasus"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".konsul.KonsulActivity"
            android:label="Konsultasi"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AboutActivity"
            android:label="Tentang"
            android:theme="@style/AppTheme" />
        <activity android:name=".kasus.DetailKasusActivity"
            android:theme="@style/AppTheme"
            android:label="Detail Kasus"></activity>
    </application>

</manifest>